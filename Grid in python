from tkinter import *

root = Tk()

mylabel1 = Label(root, text = "My Name Is Haziq, i love python coding")

mylabel2 = Label(root, text = "This how to learn grid positioning in python")

mylabel1.grid(row=0,column=0)
mylabel2.grid(row=1,column= 1)

root.mainloop()
